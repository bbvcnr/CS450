/**
 * Determines whether a member can borrow a book.
 * @param {{ membershipActive: boolean, borrowedBooksCount: number }} member
 * @param {{ available: boolean }} book
 * @returns {string} result message
 */
function canBorrowBook(member, book) {
  // TODO: implement borrowing logic
}

module.exports = { canBorrowBook };
